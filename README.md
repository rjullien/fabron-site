# ⚠️ NE PAS MODIFIER ICI

Ce dossier est un clone local du repo `rjullien/fabron-site` (output du build).

**La source de vérité est :**
- Sources MD : `epargne-famille/fabron/`
- Code build : `epargne-famille/fabron-build/`

Le CI/CD GitHub Action build et deploy automatiquement.
Ne jamais modifier les fichiers ici directement.
